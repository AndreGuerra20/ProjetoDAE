meta {
  name: EP 09 - Get ultima localizacao registada
  type: http
  seq: 18
}

get {
  url: {{uri}}sensores/cliente/1/sensor/Localizacao
  body: none
  auth: none
}

script:pre-request {
  var token = "Bearer " + bru.getEnvVar("token")
  req.setHeader("Authorization",token)
}
