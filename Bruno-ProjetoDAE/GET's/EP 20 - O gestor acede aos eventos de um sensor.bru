meta {
  name: EP 20 - O gestor acede aos eventos de um sensor
  type: http
  seq: 12
}

get {
  url: {{uri}}sensor/1/eventos
  body: none
  auth: none
}

script:pre-request {
  var token = "Bearer " + bru.getEnvVar("token")
  req.setHeader("Authorization",token)
}
