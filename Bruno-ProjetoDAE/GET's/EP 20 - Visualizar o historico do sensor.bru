meta {
  name: EP 20 - Visualizar o historico do sensor
  type: http
  seq: 6
}

get {
  url: {{uri}}sensores/1/eventos
  body: none
  auth: none
}

script:pre-request {
  var token = "Bearer " + bru.getEnvVar("token")
  req.setHeader("Authorization",token)
}
